apply plugin: "idea"

buildscript {
    repositories {
        mavenCentral()
        jcenter()
        flatDir {
            dirs 'E:/Java/PIRCCar/libs'
        }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.0.0'
    }
}

allprojects {
    repositories {
        mavenCentral()
        jcenter()
        flatDir {
            dirs 'E:/Java/PIRCCar/libs'
        }
    }
}

subprojects {
    repositories {
        mavenCentral()
        jcenter()
        flatDir {
            dirs 'E:/Java/PIRCCar/libs'
        }
    }
}